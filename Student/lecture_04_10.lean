-- properties
-- (refl r): reflexive
-- symm
-- trans
-- equiv
-- partial order

-- congruent b n mod := n % b = m % b
-- prove that congruence is an equivalence relation

-- properties of functions
-- many-to-one function
-- one-to-one injections
-- injection function

-- injective r
-- ∀ abc, r a c → r b c (then) → a = b
-- single valued property and injective property are kind of like inverses
